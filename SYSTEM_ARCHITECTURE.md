# System Architecture & Data Flow

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     USER DEVICES                             │
├─────────────────────────────────────────────────────────────┤
│  Device A              Device B              Device C         │
│  ┌──────────┐         ┌──────────┐         ┌──────────┐     │
│  │Quiz Page │         │Quiz Page │         │Quiz Page │     │
│  │[Browser] │         │[Browser] │         │[Browser] │     │
│  └──────────┘         └──────────┘         └──────────┘     │
│       │                    │                    │             │
│       │ localStorage      │ localStorage       │ localStorage│
│       ▼                    ▼                    ▼             │
│  ┌──────────┐         ┌──────────┐         ┌──────────┐     │
│  │IndexedDB │         │IndexedDB │         │IndexedDB │     │
│  │(Offline) │         │(Offline) │         │(Offline) │     │
│  └──────────┘         └──────────┘         └──────────┘     │
│       │                    │                    │             │
│       └────────────────────┼────────────────────┘             │
│                            │                                  │
└────────────────────────────┼──────────────────────────────────┘
                             │
                    /api/save-quiz-result
                    /api/quiz-results
                             │
                             ▼
            ┌────────────────────────────────┐
            │   FLASK BACKEND                │
            ├────────────────────────────────┤
            │  API Routes (results_routes.py)│
            │  ├─ POST /api/save-...        │
            │  ├─ GET /api/quiz-results     │
            │  ├─ GET /api/quiz-stats       │
            │  └─ GET /api/quiz-results/*   │
            └────────────────────────────────┘
                             │
                             ▼
            ┌────────────────────────────────┐
            │   SQLAlchemy ORM               │
            ├────────────────────────────────┤
            │  User Lookup                   │
            │  Validation                    │
            │  Stats Update                  │
            └────────────────────────────────┘
                             │
                             ▼
            ┌────────────────────────────────┐
            │   SQLite DATABASE              │
            ├────────────────────────────────┤
            │  ┌──────────────────┐          │
            │  │  quiz_results    │          │
            │  ├──────────────────┤          │
            │  │ id               │          │
            │  │ quiz_type        │ (INDEX)  │
            │  │ user_id          │ (INDEX)  │
            │  │ score            │          │
            │  │ percentage       │          │
            │  │ timestamp        │ (INDEX)  │
            │  │ user_answers     │          │
            │  └──────────────────┘          │
            │                                │
            │  ┌──────────────────┐          │
            │  │  user            │          │
            │  ├──────────────────┤          │
            │  │ id               │          │
            │  │ total_score      │ (INDEX)  │
            │  │ words_mastered   │          │
            │  │ accuracy_rate    │          │
            │  │ current_streak   │ (INDEX)  │
            │  │ last_activity    │ (INDEX)  │
            │  └──────────────────┘          │
            └────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### Quiz Completion Flow
```
User Completes Quiz
         │
         ▼
  ┌─────────────────┐
  │ finishQuiz()    │
  └─────────────────┘
         │
         ├─────────────────────────┬──────────────────────────┐
         │                         │                          │
         ▼                         ▼                          ▼
  ┌──────────────┐     ┌──────────────────┐    ┌─────────────────┐
  │ Display UI   │     │ Store in Local   │    │ Call saveToDb() │
  │ (Results)    │     │ Storage (Backup) │    │  (Async)        │
  └──────────────┘     └──────────────────┘    └─────────────────┘
                                                         │
                                                         ▼
                                          ┌──────────────────────┐
                                          │ POST /api/save-...   │
                                          ├──────────────────────┤
                                          │ • Check login        │
                                          │ • Validate data      │
                                          │ • Save to DB         │
                                          │ • Update user stats  │
                                          └──────────────────────┘
                                                         │
                                                         ▼
                                          ┌──────────────────────┐
                                          │ Return Success ✅    │
                                          │ (Quiz ID, Time)      │
                                          └──────────────────────┘
```

### Results Page Load Flow
```
User Opens /result
         │
         ▼
  ┌──────────────────┐
  │ loadResultsData()│
  └──────────────────┘
         │
         ▼
  ┌────────────────────────┐
  │ Check sessionStorage   │
  │ for current quiz       │
  └────────────────────────┘
         │
         ▼
  ┌────────────────────────┐
  │ Try Load from DB       │
  │ GET /api/quiz-results  │
  └────────────────────────┘
         │
    ┌────┴─────────┐
    │ (User Logged │
    │    In?)      │
    │              │
   YES             NO
    │              │
    ▼              ▼
 ┌────┐      ┌──────────┐
 │✅DB│      │localStorage│
 │Data│      │  Fallback  │
 └────┘      └──────────┘
    │              │
    └──────┬───────┘
           │
           ▼
  ┌──────────────────────┐
  │ displayResults()     │
  │ • Show stats        │
  │ • Show charts       │
  │ • Show history      │
  └──────────────────────┘
```

### Cross-Device Sync
```
┌─────────────┐          ┌─────────────┐          ┌─────────────┐
│  Device A   │          │  Device B   │          │  Device C   │
│  (Logged    │          │  (Logged    │          │  (Not       │
│   In)       │          │   In)       │          │   Logged)   │
└──────┬──────┘          └──────┬──────┘          └──────┬──────┘
       │                        │                        │
    Quiz 1              ┌────────┴────────┐          Quiz 2
   Saved to:            │                 │         Saved to:
   - DB    ✅           │                 │         - localStorage
   - Local              │                 │
                        ▼                 ▼
                    Same DB             Different
                    Account             Devices
                        │
                   ┌─────┴─────┐
                   │           │
                   ▼           ▼
               Same Results  Different Results
               (Synced!)    (Device C has
                           local data)
```

---

## 🔄 API Request/Response Cycle

### Save Quiz Result
```
CLIENT                          SERVER
  │                              │
  ├─ POST /api/save-...  ────────┤
  │                              │
  │  {                            │
  │    quiz_type: "words",        │
  │    score: 18,                 │
  │    ...                        │
  │  }                            │
  │                              ├─ Check @login_required
  │                              │
  │                              ├─ Validate data
  │                              │
  │                              ├─ Create QuizResult
  │                              │
  │                              ├─ Update User stats
  │                              │
  │                              ├─ Commit to DB
  │                              │
  │  ◄─────── 201 Success ───────┤
  │                              │
  │  {                            │
  │    success: true,             │
  │    quiz_id: 45,               │
  │    timestamp: "..."           │
  │  }                            │
  │                              │
```

### Fetch Quiz Results
```
CLIENT                          SERVER
  │                              │
  ├─ GET /api/quiz-results  ─────┤
  │  ?limit=50&quiz_type=words    │
  │                              │
  │                              ├─ Check @login_required
  │                              │
  │                              ├─ Query results
  │                              │  WHERE user_id = current_user.id
  │                              │  AND quiz_type = 'words'
  │                              │  ORDER BY timestamp DESC
  │                              │  LIMIT 50
  │                              │
  │                              ├─ Format response
  │                              │
  │  ◄─────── 200 Success ───────┤
  │                              │
  │  {                            │
  │    success: true,             │
  │    data: [                    │
  │      {                        │
  │        id: 45,                │
  │        quiz_type: "words",    │
  │        score: 18,             │
  │        ...                    │
  │      }                        │
  │    ],                         │
  │    total: 45,                 │
  │    returned: 10               │
  │  }                            │
  │                              │
```

---

## 📋 State Management

### Quiz Taking Session
```
┌─────────────────────────────────┐
│ Quiz Session Variables          │
├─────────────────────────────────┤
│ currentQuestion        → 0       │
│ score                  → 0       │
│ correctAnswers         → 0       │
│ incorrectAnswers       → 0       │
│ userAnswers            → []      │
│ quizData               → []      │
│ quizStarted            → false   │
│ startTime              → null    │
│ sessionId              → UUID    │
│ wordMastery            → {}      │
│ masteredWords          → Set()   │
│ reviewWords            → []      │
│ recentlyUsedWords      → Set()   │
└─────────────────────────────────┘
                │
                ├─ Quiz In Progress ──┐
                │                     │
                ▼                     ▼
    ┌──────────────────┐   ┌─────────────────────┐
    │ Update on each   │   │ Save to localStorage│
    │ question answer  │   │ every few seconds   │
    │                  │   │                     │
    │ - Score          │   │ - Quiz state        │
    │ - Answers        │   │ - Adaptive data     │
    │ - Mastery data   │   │ - Performance data  │
    └──────────────────┘   └─────────────────────┘
                │
                └─ Quiz Finished
                │
                ▼
    ┌──────────────────────────────┐
    │ Save Everything              │
    ├──────────────────────────────┤
    │ • localStorage (immediate)   │
    │ • Database (async)           │
    │ • Clear sessionStorage        │
    │ • Generate stats              │
    └──────────────────────────────┘
```

---

## 🔐 Security Flow

```
Request from Client
        │
        ▼
┌──────────────────────┐
│ Check Session/Token  │
└──────────────────────┘
        │
        ├─ Valid ──────────────┐
        │                      │
        ├─ Invalid ────────────┼─────────┐
        │                      │         │
        ▼                      ▼         ▼
    Continue      Return 401   Return
                 Unauthorized  Error
        │
        ▼
┌──────────────────────┐
│ Identify User        │
│ (current_user.id)    │
└──────────────────────┘
        │
        ▼
┌──────────────────────┐
│ Filter Query by      │
│ User ID              │
└──────────────────────┘
        │
        ▼
┌──────────────────────┐
│ Validate Input Data  │
│ • Required fields    │
│ • Data types         │
│ • Value ranges       │
└──────────────────────┘
        │
    ┌───┴─────┐
    │(Invalid)│
    │         │
   YES        NO
    │         │
    ▼         ▼
 Error    Continue
 400       │
           ▼
      ┌────────────────┐
      │ Execute Query  │
      │ or Operation   │
      └────────────────┘
           │
           ▼
      ┌────────────────┐
      │ Return Result  │
      │ (User's data   │
      │  only)         │
      └────────────────┘
```

---

## 📈 Data Persistence Strategy

```
┌─────────────────────────────────────┐
│ Quiz Completion                     │
├─────────────────────────────────────┤
│ Save to localStorage                │
│ (Immediate, guaranteed)             │
│                                     │
│ ✓ Available offline                 │
│ ✓ No network needed                 │
│ ✓ Fallback if DB fails              │
│                                     │
│ ⚠ Lost if browser cache cleared     │
│ ⚠ Device-specific                   │
└──────────────────────────────────────┘
              │
              ├─────────────┐
              │             │
              ▼             ▼
       ┌────────────┐  ┌───────────┐
       │ User Logs  │  │ Next Page  │
       │ In Later   │  │ Load       │
       └────────────┘  └───────────┘
              │             │
              ▼             ▼
       ┌────────────────────────┐
       │ Sync from localStorage │
       │ to Database (if needed) │
       └────────────────────────┘
              │
              ▼
       ┌────────────────┐
       │ Now in DB      │
       │ (Cloud backup) │
       │                │
       │ ✓ Accessible   │
       │  from any      │
       │  device        │
       │ ✓ Survives     │
       │  browser clear │
       └────────────────┘
```

---

## 🎯 Success Indicators

After implementation, you should see:

1. **Quiz Completion**
   ```
   ✅ Quiz result saved to database: {quiz_id: 45, ...}
   ```

2. **Results Page**
   ```
   📡 Attempting to load quiz history from database...
   ✅ Loaded 10 quiz results from database
   ```

3. **Cross-Device Access**
   ```
   Device A: Takes quiz, sees results immediately
   Device B: Logs in, sees same results from Device A
   ```

4. **User Stats Update**
   ```
   User.total_score increased
   User.accuracy_rate updated
   User.words_mastered increased
   User.current_streak updated
   ```

This confirms your system is working correctly! 🎉
